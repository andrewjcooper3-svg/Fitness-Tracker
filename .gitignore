# Generated design-canvas output: ~2MB of editor payload rebuilt from
# design/Main.dc.html + design/canvas.json, so the sources are tracked
# and this isn't.
design/trainer-landscape.html
design/artboards.manifest.json
